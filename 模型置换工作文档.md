# 模型置换工作文档

> 项目：LALAGAME — Last Call
> 日期：2026-08-29
> 任务：将「新置换模型素材库」中的新模型替换进 `BarPrototype/Assets/LastCall/Characters` 对应角色目录，并保证可正常出包。

---

## 1. 模型映射

| 新素材（源） | 源 ZIP | 目标角色目录 | 说明 |
|---|---|---|---|
| kiko | `kiko+model+(动效12+控制速度）.zip` | `Characters/A` | kiko → A |
| X | `x+model(含动效+控制速度）.zip` | `Characters/B` | X → B |
| 万塞 | `万塞+model(25动效+控制速度）.zip` | `Characters/C` | 万塞 → C |
| 一桐 | `一桐model(大幅度动效21+控制速度）.zip` | `Characters/D` | 一桐 → D |

> 本项目模型加载是「按目录取第一个 `.fbx`」的约定：`CastModel.Loader` 与 `SceneZeroBuilder.BuildCatalog` 都通过 `Assets/LastCall/Characters/<id>/*.fbx` 定位模型，与文件 GUID 无关。因此换模型 = 替换目录内的 FBX 与贴图。

---

## 2. 已完成的文件操作

### 2.1 拷贝进目标目录

| 目标 | 新 FBX | 贴图 |
|---|---|---|
| A（kiko） | `tripo_convert_212b3c59-1055-4f67-b115-263065d12319.fbx` | 4 张 |
| B（X） | `tripo_convert_bf66756d-e12d-4db0-b4f3-131d9e267027.fbx` | 4 张 |
| C（万塞） | `tripo_convert_2cdd8fb4-2902-4931-bae0-a1ffa8680d10.fbx` | 1 张（仅漫反射） |
| D（一桐） | `tripo_convert_90c79786-da31-4065-8831-7ba206afd87c.fbx` | 4 张 |

### 2.2 删除旧模型

| 目录 | 删除的旧文件（旧 FBX + `.meta` + 旧 `.fbm/` + `.fbm.meta`） |
|---|---|
| A | `tripo_convert_6e6a2577-b55b-4810-a821-a9abc3cdb596`（`agent1_*` 贴图） |
| B | `tripo_convert_45e0fdec-baee-407d-a3c8-8902f4109ea9`（`agent2_*` 贴图） |
| C | `tripo_convert_5d5a5cba-afe1-49f2-a66a-e35d044f8217`（`agent3_*` 贴图） |
| D | `tripo_convert_a0557a16-9a7b-4989-bef2-5c121a9134f5`（`anime_schoolgirl_*` 贴图） |

---

## 3. 最终文件结构

```
Characters/
├─ A/  tripo_convert_212b3c59-….fbx
│    └─ ….fbm/ tripo_rgb_….jpg（漫反射）、extracted_normal_….png（法线）、
│               kiko_model_…_metallic.JPEG、kiko_model_…_roughness.JPEG
├─ B/  tripo_convert_bf66756d-….fbx
│    └─ ….fbm/ tripo_rgb_….jpg（漫反射）、tripo_normal_….jpg（法线）、
│               x_model_…_metallic.JPEG、x_model_…_roughness.JPEG
├─ C/  tripo_convert_2cdd8fb4-….fbx
│    └─ ….fbm/ tripo_rgb_….jpg（仅漫反射，1 张）
└─ D/  tripo_convert_90c79786-….fbx
     └─ ….fbm/ tripo_rgb_….jpg（漫反射）、tripo_normal_….jpg（法线）、
               一桐model_…_metallic.JPEG、一桐model_…_roughness.JPEG
```

所有新 FBX 均含**骨骼蒙皮（Deformer/LimbNode）与动画（AnimationStack/Take）**：

| 模型 | AnimationStack | AnimationLayer | AnimationCurveNode | Take |
|---|---|---|---|---|
| kiko（A） | 61 | 61 | 7561 | 104 |
| X（B） | 81 | 81 | 10081 | 124 |
| 万塞（C） | 126 | 126 | 15751 | 169 |
| 一桐（D） | 106 | 106 | 13231 | 149 |

---

## 4. 代码改动（1 处）

**文件**：`BarPrototype/Assets/LastCall/Editor/SceneZeroBuilder.cs`（`BuildCatalog`，约 81 行）

**原因**：出包时角色材质由 `Cast-<id>` 单一材质驱动，该材质只从目录里按 `_basecolor.` 关键字找漫反射贴图。新模型的漫反射叫 `tripo_rgb_*.jpg`，原逻辑匹配不到会导致角色**整身变白**。

**改动**：漫反射贴图识别同时兼容 `_basecolor.` 与 `tripo_rgb`：

```csharp
// 改前
var tex = Directory.GetFiles(folder,"*",SearchOption.AllDirectories)
    .FirstOrDefault(p => p.IndexOf("_basecolor.", StringComparison.OrdinalIgnoreCase) >= 0 && !p.EndsWith(".meta"));

// 改后
var tex = Directory.GetFiles(folder,"*",SearchOption.AllDirectories)
    .FirstOrDefault(p => !p.EndsWith(".meta")
        && (p.IndexOf("_basecolor.", StringComparison.OrdinalIgnoreCase) >= 0
         || p.IndexOf("tripo_rgb", StringComparison.OrdinalIgnoreCase) >= 0));
```

> 未改动 `CastTextureImport`（法线贴图 `_normal.` 自动标记）。当前出包管线只用漫反射贴图，法线/金属/粗糙度在 `BuildCatalog` 中本就不参与最终材质，故不影响出包；如需后续启用，可把该后处理器也扩展为识别 `tripo_normal` / `extracted_normal`。

---

## 5. 仍需在 Unity 中执行的步骤（关键）

文件层面已替换，但 **Prefab 与 ArtCatalog 仍指向旧模型 GUID**，必须在 Unity 里重新生成：

1. **打开 Unity 工程**（`BarPrototype` 目录），等待导入完成。
   - Unity 会为新的 `.fbx` / `.fbm` 生成新的 `.meta`（新 GUID），并导入动画。
   - 此刻 Console 可能出现「Prefab 引用丢失」黄色警告——旧 Prefab 还指着已删除的旧模型，属预期，重建后消失。
2. **重新生成 Prefab 与 ArtCatalog**（重建 `A/B/C/D.prefab` 与 `ArtCatalog.asset`，消除旧 GUID 悬空引用）：
   - 菜单 **`Last Call / Scene 0 / 1 - Add elevator (preserve bar)`**（内部调用 `SceneZeroBuilder.Prepare()` → `BuildCatalog()`）。
   - Console 出现 **`SCENE0_PREPARED`** 即成功。
3. **重新出包验证**：
   - 菜单 **`Last Call / 2 - Build Windows MVP`**。
   - 进游戏确认：A=kiko、B=X、C=万塞、D=一桐，且贴图颜色正常（非全白）。

---

## 6. 注意事项

1. **贴图命名差异（已通过第 4 节代码修复漫反射）**
   - 旧：`*_basecolor.` / `*_normal.` / `*_metallic.` / `*_roughness.` / `*_rm.`
   - 新（Tripo 导出版）：漫反射 `tripo_rgb_*`，法线 `tripo_normal_*`（B/D）或 `extracted_normal_*`（A）。
   - 法线/金属/粗糙度当前出包管线未使用，不影响显示。
2. **万塞（C）只有漫反射贴图**：`万塞+model(25动效+控制速度）.zip` 内仅含 1 张 `tripo_rgb_*.jpg`，无法线/金属/粗糙度贴图。因出包只用漫反射，不影响显示；若后续要全 PBR 贴图需重新导出。
3. **贴图文件名不可随意重命名**
   - FBX 内部用 `RelativeFilename` 精确引用 `.fbm/<原文件名>`；重命名会破坏 FBX 内嵌材质的贴图绑定（出包虽用 `Cast-<id>` 材质不受影响，但编辑器内直接预览 FBX 会显示贴图丢失）。
4. **Prefab / ArtCatalog 必须重跑生成**（见第 5 节），否则 `Models/*.prefab` 与 `ArtCatalog.asset` 里的旧 GUID 已失效。
5. **身高适配**：`CastModel.HeightOf` 中 C=1.82、D=1.58、A/B=1.68。`Fit()` 会自动把模型等比缩放到目标身高，若观感不理想可在此处单独调整对应角色身高。
6. **旧文件已被删除**，如需要回滚，可用 Git 恢复对应角色目录。

---

## 7. 附录：源素材清单

| ZIP | FBX | 贴图数量 | 目标 |
|---|---|---|---|
| `kiko+model+(动效12+控制速度）.zip` | `tripo_convert_212b3c59-….fbx` | 4 | A |
| `x+model(含动效+控制速度）.zip` | `tripo_convert_bf66756d-….fbx` | 4 | B |
| `万塞+model(25动效+控制速度）.zip` | `tripo_convert_2cdd8fb4-….fbx` | 1 | C |
| `一桐model(大幅度动效21+控制速度）.zip` | `tripo_convert_90c79786-….fbx` | 4 | D |
