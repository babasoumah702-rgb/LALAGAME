# 未完成工作｜前端交互层级统一与三页入场改造

更新时间：2026-08-30  
状态：已按要求停止继续测试、构建和打包。

> 2026-08-31 更新：用户已明确改为需要 Windows ZIP，因此本文中“不生成 ZIP”的旧约束不再适用于最新交付。

## 本轮目标

- 新游戏改为三页单题入口：身份、目的、聊天风格。
- 游戏内剧情入口统一为“观察 / 移动 / 互动”三个一级菜单。
- 每章持续给出明确的“下一步”。
- 选择按钮只有未选择和已选择两种状态，并拦截重复提交。
- 最终只交付 Windows exe 完整运行目录，不生成 ZIP。

## 已完成代码

- 已新增服务端只读 `InteractionDto`，从章节状态、路线、姿态、事件和回复状态派生交互选项。
- 已将新游戏入口改为三页单题流程；所有答案及“跳过”一次展开，返回后保留选择，第三页选择后直接进入电梯。
- 已把赴约方式、背景、行业、当前阶段、话题偏好和在线／离线模式移入“更多设置”。
- 已建立统一的“观察 / 移动 / 互动”操作栏；线索册和暂停／存档保留在独立系统区。
- 已加入章节“下一步”提示，并覆盖 Scene 1–6 的主要推进条件。
- 已加入按“交互上下文 + 选项 ID”管理的客户端待确认状态，拦截双击和快速重复发送。
- 已加入塔罗一次性参与方式的客户端灰态及服务端锁定；第二条改选命令会被拒绝。
- 已限制等待 AI 回复时重复发送同一句话；其他不同操作仍可继续。
- 已保留先前完成的定向 AI 回复规则：未被选择的角色只旁听，不自动插嘴。
- 已更新 Scene 1 和 Scene 2→3 的可见窗口验收脚本，使其真实点击三页入口和两层菜单。

## 已获得的有效结果

- 服务端全量测试曾通过 `153 / 153`；但该结果早于最后一次“塔罗服务端锁定”改动，正式交付前必须再跑一次全量测试。
- 最后一次塔罗交互专项测试通过 `5 / 5`，包含服务端拒绝二次改选。
- 编辑器测试曾通过 `28 / 28`；最后又修改了自动化测试夹具，正式交付前需再跑一次。
- 整晚 Scene 0–6 可见 exe 流程通过 `57 / 57`：第三杯、Scene 2 自动进入塔罗、走廊、断电、真实楼梯和屋顶均通过。
- 1280×720 的三页入口及 Scene 1 完整可见流程通过 `41 / 41`。
- 先前的 1920×1080 和 1280×800 快速界面检查各通过 `32 / 32`，但最终构建后的对应复跑被中途停止，不能作为最终交付证据。

## 尚未完成

1. **最终全量回归**
   - 重新运行服务端全部 153 项测试。
   - 重新运行编辑器全部 28 项测试。
   - 确认最后修改后没有 C# 编译错误。

2. **剩余可见窗口验收**
   - 1920×1080 的三页入口和统一菜单复跑。
   - 1280×800 的三页入口和统一菜单复跑。
   - Scene 2→塔罗真实 UI 点击流程复跑。
   - Scene 0 回归复跑。
   - 旧卡牌入口回归复跑。
   - 当前 1080p 用例在启动后被停止，现有目录中的报告可能是旧结果或不完整结果，必须覆盖重跑。

3. **最终运行目录**
   - 当前候选构建在 `Builds/Scene0-Windows`，尚未完成最终白名单复制和交付命名。
   - 尚未生成新的唯一命名 Windows 完整运行目录。
   - 尚未对最终目录执行密钥、存档、日志、测试脚本和调试产物排除检查。
   - 尚未对最终目录中的 exe 做启动冒烟测试。
   - 尚未计算并记录最终 exe 的 SHA-256。
   - 按用户要求，不应生成 ZIP。

4. **交付说明**
   - 尚未整理本轮最终测试汇总、已知限制和启动说明。
   - 尚未把最终运行目录与 SHA-256 发给用户。

## 继续工作时的建议顺序

1. 运行服务端 153 项测试和编辑器 28 项测试。
2. 运行以下可见窗口用例：1080p、1280×800、Scene 2→塔罗、Scene 0、旧卡牌。
3. 检查所有报告均为本次构建生成，不能复用旧报告。
4. 使用 `Tools/Package-FullNight-Exe.ps1` 生成新的唯一命名完整运行目录。
5. 对该目录执行白名单审计与 1280×720 可见窗口冒烟测试。
6. 计算 `LastCall.exe` 的 SHA-256；只交付运行目录，不创建 ZIP。

## 主要相关文件

- `Server/src/interaction.ts`
- `Server/src/view.ts`
- `Server/src/scene-three.ts`
- `Server/src/tests/interaction.test.ts`
- `Assets/LastCall/Runtime/Contracts.cs`
- `Assets/LastCall/Runtime/LastCallEntry.cs`
- `Assets/LastCall/Runtime/UnifiedInteractionInterface.cs`
- `Assets/LastCall/Runtime/LastCallWorldUI.cs`
- `Assets/LastCall/Runtime/SceneOneVerification.cs`
- `Assets/LastCall/Runtime/SceneTwoThreeVerification.cs`
- `Tools/Verify-FullNight-Windows.ps1`
- `Tools/Package-FullNight-Exe.ps1`

## 当前注意事项

- 工作区包含此前 Scene 0–6、AI、Humanoid 模型和场景的较多未提交改动，不要用清理或重置命令覆盖它们。
- `Builds/Scene0-Windows` 只是候选构建源，不应直接当作最终交付目录。
- 最终验收必须使用可见窗口；隐藏窗口黑图不算通过证据。
- 不要生成 ZIP。
